%nav.navbar-dark.bg-dark
  .container
    .row
      .nav-item= link_to "Home",   root_path, :class => "nav-link"

      -if current_user.blank?
        .nav-item= link_to 'Login', login_path, :class => "nav-link"
        .nav-item= link_to 'Signup', signup_path, :class => "nav-link"
      -else
        .nav-item= link_to 'Edit Profile', edit_user_registration_path, :class => "nav-link"
        .nav-item= link_to 'Log out', destroy_user_session_path, method: :delete, :class => "nav-link"


